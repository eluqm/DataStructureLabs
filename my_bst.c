/** Implementación de la estructura de datos BST
 *
 *@autor TU NOMBRE
 * Semestre TU SEMESTRE
*/

#include "my_bst.h"





/**
 * Comprueba si el valor existe en el árbol.
 * devuelve verdadero si el valor existe en el árbol.
 * Falso en caso contrario.
*/
bool bst_exists(BST *tree, int value)
{
    return false;
}

/**
 * Devuelve el tamaño del árbol.
*/
unsigned int bst_size(BST *tree) {
    return 0;
}

/**
 * Agrega un valor al árbol.
 * devuelve 1 si el valor se agregó correctamente.
 * devuelve 0 si el valor ya existe en el árbol.
 * devuelve -1 si el valor no se pudo agregar debido a errores. (Malloc falló)
*/
int bst_add(BST *tree, int value) {
    return 0;
  
}

/**
 * Libera la memoria asignada para el árbol.
 * pista: Piense en qué orden funciona para el recorrido (pre, in, post) y cómo
 * puede ayudarte a liberar la memoria
*/
void bst_free(BST *tree) {

}

/**
 * Crea un nuevo BST.
 * devuelve un puntero al nuevo BST.
 * El nodo raíz seguirá siendo NULL hasta que se llame al primer bst_add
*/
BST *create_bst() {
  
    return NULL;
}